<Page x:Class="Mahu.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="SettingsPage">

    <Grid Background="White">
        <!-- Shadow từ Menu đổ sang -->
        <Rectangle HorizontalAlignment="Left" Width="10" Panel.ZIndex="999">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#1A000000" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Cài đặt hệ thống" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Placeholder Content -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:SymbolIcon Symbol="Settings24" FontSize="64" Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBlock Text="Tùy chỉnh ứng dụng Mahu" FontSize="18" Foreground="#999999" HorizontalAlignment="Center"/>
                <TextBlock Text="(Chức năng đang được phát triển)" FontSize="14" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
