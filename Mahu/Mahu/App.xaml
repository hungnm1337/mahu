<Application x:Class="Mahu.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Dictionaries của WPF-UI sẽ được code add vào trong App.xaml.cs -->
            </ResourceDictionary.MergedDictionaries>

            <!-- CSS: Label cho các trường nhập liệu -->
            <Style x:Key="InputLabelStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="Foreground" Value="#333333"/>
            </Style>

            <!-- CSS: Khung bọc ô nhập liệu (Card Border) -->
            <Style x:Key="InputBorderCardStyle" TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="1" Direction="270" Opacity="0.1" Color="Black"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- CSS: Khung hiển thị dữ liệu lớn (Preview Area) -->
            <Style x:Key="PreviewBorderCardStyle" TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270" Opacity="0.08" Color="Black"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- CSS: Ô nhập liệu không viền (nằm trong InputBorderCardStyle) -->
            <Style x:Key="BorderlessInputStyle" TargetType="Control">
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="8,6"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
